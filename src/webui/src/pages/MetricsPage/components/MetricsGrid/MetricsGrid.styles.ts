import { makeStyles } from "@mui/styles";

export const useMetricsGridStyles = makeStyles(() => ({
  root: {
    display: "flex",
    flexDirection: "column",
    gap: 1,
    height: "100%",
  },
}));
