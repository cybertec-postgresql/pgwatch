apiVersion: 1

datasources:
- name: pg-metrics
  type: postgres
  url: postgres:5432
  access: proxy
  password: pgwatch3admin
  user: pgwatch3
  database: pgwatch3_metrics
  basicAuth: false
  isDefault: true
  jsonData:
     sslmode: disable
     postgresVersion: 1500 
  version: 1
  editable: true
