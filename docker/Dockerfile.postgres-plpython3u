FROM postgres:18

# Install the necessary packages for plpython3u
RUN apt-get update && apt-get install -y \
    postgresql-plpython3-18 postgresql-18-pg-qualstats python3-psutil \
    pg-activity

# Clean up
RUN apt-get clean && rm -rf /var/lib/apt/lists/*